# CLAUDE.md

This repo's actual instructions live in [`AGENTS.md`](./AGENTS.md) — read that
file, not this one. It's shared across every AI tool used on this repo so
there's one source of truth instead of a Claude-specific copy that drifts.

Claude Code-specific role definitions still live in `.claude/agents/` as
before — those describe *how* Claude Code does the work; `AGENTS.md`
describes *what* counts as done.

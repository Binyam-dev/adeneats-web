# Supabase — anon key only, used client-side to insert into launch_waitlist.
# RLS restricts this key to INSERT (see supabase/launch_waitlist.sql).
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# App Store listing — placeholder until the October 2026 launch. When unset,
# "Get the app" buttons fall back to the waitlist form instead of a dead link.
APP_STORE_URL=
